[project]
name = "ledgerline"
version = "0.1.0"
description = "Real-time voice assistant that plans your next 30 days of money."
requires-python = ">=3.11,<3.13"
dependencies = [
  "pipecat-ai[daily,deepgram]==1.9.0",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "pydantic>=2.7",
  "pydantic-settings>=2.3",
  "python-dotenv>=1.0",
  "loguru>=0.7",
  "httpx>=0.27",
]

[dependency-groups]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.24",
  "pytest-httpx>=0.30",
  "ruff>=0.6",
  "import-linter>=2",
  "pyyaml>=6",
  "hypothesis>=6",
  "playwright>=1.47",
  "tiktoken>=0.14.0",
]
evals = [
  "openai>=1.50",
  "pyyaml>=6",
]

[tool.uv]
default-groups = ["dev"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
  "llm: calls a paid LLM, needs OPENAI_API_KEY",
  "e2e: drives a browser with Playwright",
  "voice: needs live Daily/Deepgram/Cartesia keys",
]
addopts = "-m 'not llm and not e2e and not voice' -q"

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["docs", "frontend", ".claude"]  # .claude holds editor hooks and a vendored skill, not product code

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.importlinter]
root_package = "ledgerline"
include_external_packages = true

[[tool.importlinter.contracts]]
name = "layers: api -> voice -> agent -> domain"
type = "layers"
layers = ["ledgerline.api", "ledgerline.voice", "ledgerline.agent", "ledgerline.domain"]

[[tool.importlinter.contracts]]
name = "domain is pure"
type = "forbidden"
source_modules = ["ledgerline.domain"]
forbidden_modules = ["pipecat", "openai", "fastapi", "httpx", "ledgerline.agent", "ledgerline.voice", "ledgerline.api"]

[[tool.importlinter.contracts]]
name = "agent never imports pipecat"
type = "forbidden"
source_modules = ["ledgerline.agent"]
forbidden_modules = ["pipecat", "fastapi"]


[[tool.importlinter.contracts]]
name = "domain internal layers: cards -> engine -> state -> policy -> models"
type = "layers"
layers = [
  "ledgerline.domain.cards",
  "ledgerline.domain.engine",
  "ledgerline.domain.state",
  "ledgerline.domain.policy",
  "ledgerline.domain.models",
]
